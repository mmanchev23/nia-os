apiVersion: v1
kind: Service
metadata:
  name: nia-os
  namespace: nia-os
  labels:
    app: nia-os
  annotations:
    argocd.argoproj.io/sync-wave: "3"
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: 8000
  selector:
    app: nia-os
